# v1.1.1 更新日志

**日期：** 2026-06-29

- 修复 `_judging_groups` 并发竞争导致的多余 LLM 调用
- 修复 `/reply` 多群同时开关导致状态丢失
- 修复开关文件崩溃损坏问题（改为原子写入）
- 修复关闭 `enabled` 后仍消耗 token 的问题
- 修复缓存清理频率翻倍问题
- 修复写路径缓存未清理导致内存泄漏
- 修复缓存写入时窗口期多余 LLM 调用（double-check）
- 修复 `bool("false")` 误判为 `True` 导致拦截失败
- 修复 `toggle_reply()` 内 yield 导致锁泄漏
- 修复 `terminate()` 卸载时与 `/reply` 的竞态
- 修复群 ID `"0"` 被误判为非群消息
- 修复缓存清理异常导致消息处理中断
- 补全概率放行日志缺失的 confidence 字段
- 补全 `_conf_schema.json` 中 `{sender}` 变量说明

---

## 文件变更清单

| 文件 | 变更 |
|------|------|
| `main.py` | 新增 `_judging_groups_lock` + `_switch_lock`；`on_llm_request` enabled 检查；原子写文件；清理路径修复；double-check；`_normalize_result` 类型防御；`yield` 移出锁；`terminate()` 加锁；`_get_group_id` 修复；`_cleanup_expired_cache` 异常保护；confidence 日志补全；版本号 → `1.1.1` |
| `_conf_schema.json` | 配置描述补充 `{sender}`；默认 Prompt 模板新增发送者信息 |
| `metadata.yaml` | `version: v1.1.1` |
| `README.md` | 补充 v1.1.1 更新日志 |
| `README_EN.md` | 补充 v1.1.1 更新日志 |
| `log111.md` | 本次更新日志文件（本文件） |
